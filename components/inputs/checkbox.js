import {Empty} from "mfc-core";

export default function checkbox() {
    return [{

        headers: [
            {
                content: (
                    <Empty customLabel={'Coming soon'}/>
                ),
                type: 'native-code'
            },
        ]
    }]
}